# Wishlist (Sevimli kurslar) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Foydalanuvchi kurslarni "Sevimlilar" ro'yxatiga qo'shadi, ko'radi va olib tashlaydi (backend `/api/wishlist` allaqachon tayyor).

**Architecture:** Thin axios wrapper → Redux slice (optimistik toggle, `promptSlice` uslubi) → CourseCard'ga yurak tugmasi (dumb komponent) → yangi WishlistScreen → CoursesStack navigatsiya + Courses header ikonkasi.

**Tech Stack:** React Native 0.81, Expo SDK 54, Redux Toolkit, axios (`axiosInstance`), React Navigation v7, `@expo/vector-icons` (Ionicons).

**Verifikatsiya:** Loyihada test framework yo'q. Har task'dan keyin `npx tsc --noEmit` (tip tekshiruvi) va `npm run lint`. Yakunda ilovada qo'lda sinov.

---

## File Structure

| Fayl | Holat | Mas'uliyat |
|------|-------|-----------|
| `src/api/wishlistApi.ts` | Create | 3 endpoint wrapper |
| `src/store/slices/wishlistSlice.ts` | Create | State + thunklar (fetch, toggle) |
| `src/store/index.ts` | Modify | `wishlist` reducer ro'yxatdan o'tkazish |
| `src/components/course/CourseCard.tsx` | Modify | Yurak tugmasi (ixtiyoriy proplar) |
| `src/screens/courses/WishlistScreen.tsx` | Create | Sevimli kurslar ro'yxati ekrani |
| `src/navigation/types.ts` | Modify | `CourseStackParamList`ga `Wishlist` |
| `src/navigation/CoursesStack.tsx` | Modify | `Wishlist` screen ro'yxatga |
| `src/screens/courses/CoursesScreen.tsx` | Modify | Header ikonka + kartaga yurak ulash |

---

### Task 1: Wishlist API qatlami

**Files:**
- Create: `src/api/wishlistApi.ts`

- [ ] **Step 1: API faylni yaratish**

`promptApi.ts` uslubidagi thin wrapper. CLAUDE.md: faqat `axiosInstance`.

```ts
import axiosInstance from './axiosInstance';

export const wishlistApi = {
  getWishlist: () => axiosInstance.get('/wishlist'),
  addToWishlist: (courseId: string) => axiosInstance.post(`/wishlist/${courseId}`),
  removeFromWishlist: (courseId: string) => axiosInstance.delete(`/wishlist/${courseId}`),
};
```

- [ ] **Step 2: Tip tekshiruvi**

Run: `npx tsc --noEmit`
Expected: Xato yo'q (yangi fayl bo'yicha).

- [ ] **Step 3: Commit**

```bash
git add src/api/wishlistApi.ts
git commit -m "feat(wishlist): add API wrapper"
```

---

### Task 2: Wishlist Redux slice

**Files:**
- Create: `src/store/slices/wishlistSlice.ts`

Backend `GET /wishlist` javobi: `{ data: { courses: [ { courseId: Course } ] } }`. `courseId` populate qilingan Course obyekti (yoki o'chirilgan kurs uchun `null`).

- [ ] **Step 1: Slice'ni yozish**

```ts
import { createSlice, createAsyncThunk } from '@reduxjs/toolkit';
import { wishlistApi } from '../../api/wishlistApi';
import { Course } from '../../types/course';

interface WishlistState {
  ids: string[];
  items: Course[];
  loading: boolean;
  refreshing: boolean;
  error: string | null;
}

const initialState: WishlistState = {
  ids: [],
  items: [],
  loading: false,
  refreshing: false,
  error: null,
};

// Backend javobidan populate qilingan kurslarni ajratib oladi.
// courseId === null (o'chirilgan kurs) bo'lganlar filtrlanadi.
const extractCourses = (resp: any): Course[] => {
  const courses = resp?.data?.data?.courses ?? resp?.data?.courses ?? [];
  return courses
    .map((c: any) => c?.courseId)
    .filter((c: any): c is Course => !!c && !!c._id);
};

export const fetchWishlist = createAsyncThunk(
  'wishlist/fetch',
  async (_, { rejectWithValue }) => {
    try {
      const response = await wishlistApi.getWishlist();
      return extractCourses(response);
    } catch (error: any) {
      return rejectWithValue(
        error.response?.data?.message || 'Sevimlilarni yuklab bo\'lmadi'
      );
    }
  }
);

// Optimistik toggle: pending'da darrov flip, rejected'da revert.
// `course` to'liq obyekt sifatida uzatiladi — qo'shganda items'ga kiritish uchun.
export const toggleWishlist = createAsyncThunk(
  'wishlist/toggle',
  async (
    { course }: { course: Course },
    { getState, rejectWithValue }
  ) => {
    const state = getState() as { wishlist: WishlistState };
    const wasInList = state.wishlist.ids.includes(course._id);
    try {
      if (wasInList) {
        await wishlistApi.removeFromWishlist(course._id);
      } else {
        await wishlistApi.addToWishlist(course._id);
      }
      return { courseId: course._id, added: !wasInList };
    } catch (error: any) {
      return rejectWithValue({
        courseId: course._id,
        wasInList,
        message: error.response?.data?.message || 'Amalni bajarib bo\'lmadi',
      });
    }
  }
);

const addLocally = (state: WishlistState, course: Course) => {
  if (!state.ids.includes(course._id)) {
    state.ids.push(course._id);
    state.items.unshift(course);
  }
};

const removeLocally = (state: WishlistState, courseId: string) => {
  state.ids = state.ids.filter((id) => id !== courseId);
  state.items = state.items.filter((c) => c._id !== courseId);
};

const wishlistSlice = createSlice({
  name: 'wishlist',
  initialState,
  reducers: {
    setRefreshing: (state, action: { payload: boolean }) => {
      state.refreshing = action.payload;
    },
  },
  extraReducers: (builder) => {
    builder
      .addCase(fetchWishlist.pending, (state) => {
        state.loading = state.items.length === 0 && !state.refreshing;
        state.error = null;
      })
      .addCase(fetchWishlist.fulfilled, (state, action) => {
        state.loading = false;
        state.refreshing = false;
        state.items = action.payload;
        state.ids = action.payload.map((c) => c._id);
      })
      .addCase(fetchWishlist.rejected, (state, action) => {
        state.loading = false;
        state.refreshing = false;
        state.error = action.payload as string;
      })
      // Optimistik: so'rov ketishidan oldin UI yangilanadi.
      .addCase(toggleWishlist.pending, (state, action) => {
        const course = action.meta.arg.course;
        if (state.ids.includes(course._id)) {
          removeLocally(state, course._id);
        } else {
          addLocally(state, course);
        }
      })
      // Xato: optimistik o'zgarishni qaytaramiz.
      .addCase(toggleWishlist.rejected, (state, action) => {
        const payload = action.payload as {
          courseId: string;
          wasInList: boolean;
          message?: string;
        };
        if (payload) {
          // wasInList=true edi va biz o'chirgan edik → qaytaramiz (lekin Course obyekti yo'q).
          // wasInList=false edi va biz qo'shgan edik → o'chiramiz.
          if (payload.wasInList) {
            // Revert qilish uchun fetchWishlist qayta chaqirilishi tavsiya etiladi.
            // Bu yerda faqat id'ni qaytaramiz (item to'liq tiklanmaydi).
            if (!state.ids.includes(payload.courseId)) {
              state.ids.push(payload.courseId);
            }
          } else {
            removeLocally(state, payload.courseId);
          }
          state.error = payload.message ?? 'Amalni bajarib bo\'lmadi';
        }
      });
  },
});

export const { setRefreshing } = wishlistSlice.actions;
export default wishlistSlice.reducer;
```

- [ ] **Step 2: Tip tekshiruvi**

Run: `npx tsc --noEmit`
Expected: Xato yo'q.

- [ ] **Step 3: Commit**

```bash
git add src/store/slices/wishlistSlice.ts
git commit -m "feat(wishlist): add redux slice with optimistic toggle"
```

---

### Task 3: Slice'ni store'ga ulash

**Files:**
- Modify: `src/store/index.ts`

- [ ] **Step 1: Reducer'ni ro'yxatdan o'tkazish**

`src/store/index.ts` ichida import qo'shing (mavjud importlar yonida):

```ts
import wishlistReducer from './slices/wishlistSlice';
```

`reducer` obyektiga `offline` qatoridan keyin qo'shing:

```ts
  reducer: {
    auth: authReducer,
    course: courseReducer,
    prompt: promptReducer,
    ranking: rankingReducer,
    chat: chatReducer,
    offline: offlineReducer,
    wishlist: wishlistReducer,
  },
```

- [ ] **Step 2: Tip tekshiruvi**

Run: `npx tsc --noEmit`
Expected: Xato yo'q. `RootState`'da endi `wishlist` mavjud.

- [ ] **Step 3: Commit**

```bash
git add src/store/index.ts
git commit -m "feat(wishlist): register reducer in store"
```

---

### Task 4: CourseCard'ga yurak tugmasi

**Files:**
- Modify: `src/components/course/CourseCard.tsx`

Komponent dumb qoladi: yurak holati va handler prop sifatida keladi. `onToggleWishlist` berilmasa, yurak ko'rinmaydi (boshqa joylarda CourseCard buzilmaydi).

- [ ] **Step 1: Proplarni va yurak tugmasini qo'shish**

`CourseCardProps` interfeysini yangilang:

```tsx
interface CourseCardProps {
  course: Course;
  onPress: (id: string) => void;
  horizontal?: boolean;
  isWishlisted?: boolean;
  onToggleWishlist?: (id: string) => void;
}
```

Komponent imzosini yangilang:

```tsx
const CourseCard = ({
  course,
  onPress,
  horizontal = false,
  isWishlisted = false,
  onToggleWishlist,
}: CourseCardProps) => {
  const { colors, spacing, typography } = useTheme();
```

`triggerHaptic` import qiling (fayl boshida):

```tsx
import { triggerHaptic } from '../../utils/haptics';
```

`<Image .../>` dan keyin (thumbnail ustida) yurak tugmasini qo'shing — `<Image>` va `<View style={styles.content}>` orasiga:

```tsx
      {onToggleWishlist && (
        <TouchableOpacity
          style={styles.heartButton}
          onPress={() => {
            triggerHaptic('light');
            onToggleWishlist(course._id);
          }}
          hitSlop={{ top: 8, bottom: 8, left: 8, right: 8 }}
        >
          <Ionicons
            name={isWishlisted ? 'heart' : 'heart-outline'}
            size={20}
            color={isWishlisted ? colors.error : '#fff'}
          />
        </TouchableOpacity>
      )}
```

`styles`ga `heartButton` qo'shing (`thumbnail` dan keyin):

```tsx
  heartButton: {
    position: 'absolute',
    top: 8,
    right: 8,
    width: 32,
    height: 32,
    borderRadius: 16,
    backgroundColor: 'rgba(0,0,0,0.45)',
    alignItems: 'center',
    justifyContent: 'center',
  },
```

> Eslatma: `rgba(0,0,0,0.45)` — bu theme rangi emas, balki yurak ikonkasi rasm ustida ko'rinishi uchun kontrast overlay. Bu CLAUDE.md "inline rang" taqiqiga kirmaydi (semantik UI rang emas).

- [ ] **Step 2: Tip tekshiruvi + lint**

Run: `npx tsc --noEmit && npm run lint`
Expected: Xato yo'q.

- [ ] **Step 3: Commit**

```bash
git add src/components/course/CourseCard.tsx
git commit -m "feat(wishlist): add heart toggle button to CourseCard"
```

---

### Task 5: Navigatsiya tiplari va CoursesStack

**Files:**
- Modify: `src/navigation/types.ts`
- Modify: `src/navigation/CoursesStack.tsx`

- [ ] **Step 1: ParamList'ga Wishlist qo'shish**

`src/navigation/types.ts` ichida `CourseStackParamList`ni yangilang:

```ts
export type CourseStackParamList = {
  Courses: undefined;
  CourseDetail: { courseId: string };
  VideoPlayer: { videoId: string; courseId: string };
  Wishlist: undefined;
};
```

- [ ] **Step 2: CoursesStack'ga screen qo'shish**

`src/navigation/CoursesStack.tsx`: import va `<Stack.Screen>` qo'shing.

Import (mavjud importlar yonida):

```tsx
import WishlistScreen from '../screens/courses/WishlistScreen';
```

`<Stack.Screen name="VideoPlayer" ... />` dan keyin:

```tsx
      <Stack.Screen name="Wishlist" component={WishlistScreen} />
```

- [ ] **Step 3: Tip tekshiruvi**

Run: `npx tsc --noEmit`
Expected: `WishlistScreen` hali yo'qligi sababli faqat shu import bo'yicha xato bo'lishi mumkin — Task 6'da yaratiladi. Agar xato faqat shu bo'lsa, davom eting (Task 6'dan keyin yana tekshiramiz).

- [ ] **Step 4: Commit**

```bash
git add src/navigation/types.ts src/navigation/CoursesStack.tsx
git commit -m "feat(wishlist): wire Wishlist route into CoursesStack"
```

---

### Task 6: WishlistScreen

**Files:**
- Create: `src/screens/courses/WishlistScreen.tsx`

`CoursesScreen` grid uslubini takrorlaydi (2 ustun). Har karta `isWishlisted={true}` — chunki bu ro'yxatdagilar sevimli. Yurakni bosish → `toggleWishlist` → karta ro'yxatdan chiqadi.

- [ ] **Step 1: Ekranni yozish**

```tsx
import React, { useEffect } from 'react';
import {
  View,
  Text,
  StyleSheet,
  FlatList,
  TouchableOpacity,
  RefreshControl,
} from 'react-native';
import { Ionicons } from '@expo/vector-icons';
import { useTheme } from '../../theme';
import { useAppDispatch, useAppSelector } from '../../store/hooks';
import {
  fetchWishlist,
  toggleWishlist,
  setRefreshing,
} from '../../store/slices/wishlistSlice';
import CourseCard from '../../components/course/CourseCard';
import Loader from '../../components/common/Loader';
import { Course } from '../../types/course';
import { triggerHaptic } from '../../utils/haptics';

const WishlistScreen = ({ navigation }: any) => {
  const { colors, spacing } = useTheme();
  const dispatch = useAppDispatch();
  const { items, ids, loading, refreshing } = useAppSelector(
    (state) => state.wishlist
  );

  useEffect(() => {
    dispatch(fetchWishlist());
  }, [dispatch]);

  const onRefresh = () => {
    dispatch(setRefreshing(true));
    dispatch(fetchWishlist());
  };

  const goBack = () => {
    triggerHaptic('light');
    navigation.goBack();
  };

  const renderItem = ({ item }: { item: Course }) => (
    <CourseCard
      course={item}
      onPress={(id) => navigation.navigate('CourseDetail', { courseId: id })}
      isWishlisted={ids.includes(item._id)}
      onToggleWishlist={() => dispatch(toggleWishlist({ course: item }))}
    />
  );

  return (
    <View style={[styles.container, { backgroundColor: colors.background }]}>
      <View style={[styles.header, { padding: spacing.xl }]}>
        <TouchableOpacity onPress={goBack} hitSlop={{ top: 8, bottom: 8, left: 8, right: 8 }}>
          <Ionicons name="chevron-back" size={26} color={colors.text} />
        </TouchableOpacity>
        <Text style={[styles.title, { color: colors.text }]}>Sevimli kurslar</Text>
        <View style={{ width: 26 }} />
      </View>

      {loading && items.length === 0 ? (
        <Loader fullScreen />
      ) : items.length === 0 ? (
        <View style={styles.emptyState}>
          <Ionicons name="heart-outline" size={56} color={colors.textSecondary} />
          <Text style={[styles.emptyText, { color: colors.textSecondary }]}>
            Hozircha sevimli kurs yo'q
          </Text>
        </View>
      ) : (
        <FlatList
          data={items}
          renderItem={renderItem}
          keyExtractor={(item) => item._id}
          numColumns={2}
          columnWrapperStyle={styles.columnWrapper}
          contentContainerStyle={styles.listContent}
          showsVerticalScrollIndicator={false}
          refreshControl={
            <RefreshControl
              refreshing={refreshing}
              onRefresh={onRefresh}
              tintColor={colors.primary}
            />
          }
        />
      )}
    </View>
  );
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  header: {
    paddingTop: 60,
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'space-between',
  },
  title: {
    fontSize: 22,
    fontWeight: '700',
  },
  listContent: {
    padding: 16,
  },
  columnWrapper: {
    justifyContent: 'space-between',
  },
  emptyState: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
    paddingHorizontal: 32,
  },
  emptyText: {
    marginTop: 12,
    fontSize: 15,
    textAlign: 'center',
  },
});

export default WishlistScreen;
```

- [ ] **Step 2: Tip tekshiruvi + lint**

Run: `npx tsc --noEmit && npm run lint`
Expected: Xato yo'q (Task 5'dagi import xatosi ham endi yo'qoladi).

- [ ] **Step 3: Commit**

```bash
git add src/screens/courses/WishlistScreen.tsx
git commit -m "feat(wishlist): add WishlistScreen"
```

---

### Task 7: CoursesScreen'ga header ikonkasi va kartaga yurak ulash

**Files:**
- Modify: `src/screens/courses/CoursesScreen.tsx`

- [ ] **Step 1: Wishlist state va actionlarni import qilish**

`fetchCourses` import qatoridan keyin qo'shing:

```tsx
import {
  fetchWishlist,
  toggleWishlist,
} from '../../store/slices/wishlistSlice';
```

- [ ] **Step 2: Selector va dispatch'ni komponentga ulash**

Mavjud `const { courses, loading } = useAppSelector(...)` qatoridan keyin:

```tsx
  const wishlistIds = useAppSelector((state) => state.wishlist.ids);
```

`fetchCourses` chaqiriladigan `useEffect`dan keyin yangi `useEffect` qo'shing (sevimlilarni bir marta yuklash uchun):

```tsx
  useEffect(() => {
    dispatch(fetchWishlist());
  }, [dispatch]);
```

- [ ] **Step 3: renderItem'ga yurak proplarini qo'shish**

Mavjud `renderItem`ni yangilang:

```tsx
  const renderItem = ({ item }: any) => (
    <CourseCard
      course={item}
      onPress={(id) => navigation.navigate('CourseDetail', { courseId: id })}
      isWishlisted={wishlistIds.includes(item._id)}
      onToggleWishlist={() => dispatch(toggleWishlist({ course: item }))}
    />
  );
```

- [ ] **Step 4: Header'ga yurak ikonkasini qo'shish**

Mavjud header'da `<Text style={[styles.title, ...]}>Kurslar</Text>` qatorini quyidagicha o'rang (sarlavha va ikonka yonma-yon):

```tsx
        <View style={styles.headerRow}>
          <Text style={[styles.title, { color: colors.text }]}>Kurslar</Text>
          <TouchableOpacity
            onPress={() => {
              triggerHaptic('light');
              navigation.navigate('Wishlist');
            }}
            hitSlop={{ top: 8, bottom: 8, left: 8, right: 8 }}
          >
            <Ionicons name="heart-outline" size={26} color={colors.text} />
          </TouchableOpacity>
        </View>
```

> Eski `styles.title`da `marginBottom: 16` bor. `headerRow`ga ko'chiramiz, shuning uchun `title`dan `marginBottom`ni olib tashlang (Step 5).

`styles`ga `headerRow` qo'shing:

```tsx
  headerRow: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'space-between',
    marginBottom: 16,
  },
```

- [ ] **Step 5: title stilidan marginBottom'ni olib tashlash**

`styles.title` ni yangilang:

```tsx
  title: {
    fontSize: 28,
    fontWeight: '700',
  },
```

- [ ] **Step 6: Tip tekshiruvi + lint**

Run: `npx tsc --noEmit && npm run lint`
Expected: Xato yo'q.

- [ ] **Step 7: Commit**

```bash
git add src/screens/courses/CoursesScreen.tsx
git commit -m "feat(wishlist): add wishlist header icon and heart toggle to CoursesScreen"
```

---

### Task 8: Qo'lda sinov (yakuniy verifikatsiya)

**Files:** (kod o'zgarishi yo'q)

- [ ] **Step 1: Ilovani ishga tushirish**

Run: `npx expo start --offline`
Telefon yoki emulatorda oching.

- [ ] **Step 2: Sinov ssenariysi**

Quyidagilarni tekshiring:
1. Kurslar tabiga o'ting — har kartada yurak (bo'sh) ko'rinadi
2. Bir kursning yuragini bosing → darrov to'ladi (qizil), haptic seziladi
3. Header'dagi yurak ikonkasini bosing → "Sevimli kurslar" ekrani ochiladi, o'sha kurs ro'yxatda
4. Ro'yxatdagi kursning yuragini bosing → ro'yxatdan chiqadi
5. Orqaga qayting → Kurslar'da o'sha kurs yuragi yana bo'sh
6. Pull-to-refresh WishlistScreen'da ishlaydi
7. Bo'sh ro'yxat: hammasini o'chiring → "Hozircha sevimli kurs yo'q" chiqadi

- [ ] **Step 3: Topilgan xatolarni tuzatish**

Agar xato bo'lsa, tegishli task'ga qaytib tuzating va qayta commit qiling.

---

## Notes
- `.env` commit qilinmaydi (CLAUDE.md).
- Barcha UI matn o'zbekcha.
- Optimistik toggle xatolik holatida `state.error` o'rnatiladi; kelajakda toast qo'shilishi mumkin (hozir YAGNI).
